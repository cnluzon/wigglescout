# Calculate bigWig coverage per chromosome

Calculate bigWig coverage per chromosome

## Usage

``` r
bw_chr_coverage(bwfile, default_na = NA_real_)
```

## Arguments

- bwfile:

  bigWig file

- default_na:

  Value to replace missing values.
