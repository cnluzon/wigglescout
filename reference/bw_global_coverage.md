# Calculate bigWig global coverage.

Calculate bigWig global coverage.

## Usage

``` r
bw_global_coverage(bwfile, default_na = NA_real_)
```

## Arguments

- bwfile:

  bigWig file

- default_na:

  Value to replace missing values.
