# Density 2d histogram plot

Plots a 2d histogramfrom a GRanges object that already has x and y
columns.

## Usage

``` r
.density_body(gr, binwidth = 0.05)
```

## Arguments

- gr:

  GRanges

- binwidth:

  Size of the bin in histogram (resolution of the plot)

## Value

A ggplot object
