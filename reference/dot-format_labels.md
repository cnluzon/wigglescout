# Shorten labels to a max length and verify no repeated elements are produced

Shorten labels to a max length and verify no repeated elements are
produced

## Usage

``` r
.format_labels(labels, max_length = 35)
```

## Arguments

- labels:

  List of labels

- max_length:

  Number of characters the shorten labels can have (35)

## Value

An array of strings
