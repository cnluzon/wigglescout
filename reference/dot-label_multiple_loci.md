# Label loci with a list of labels

If some labels are duplicated, numbers are added to the end

## Usage

``` r
.label_multiple_loci(loci, labels)
```

## Arguments

- loci:

  List of loci

- labels:

  List of labels

## Value

A list of labels
