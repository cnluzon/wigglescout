# Make a string out of a named list. Split into lines if too wide.

Make a string out of a named list. Split into lines if too wide.

## Usage

``` r
.limited_size_caption_line(named_list)
```

## Arguments

- named_list:

  A named list

## Value

A string
