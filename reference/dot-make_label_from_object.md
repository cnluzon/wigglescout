# Get a valid label

Get a valid label

## Usage

``` r
.make_label_from_object(obj, max_length = 35)
```

## Arguments

- obj:

  Something to convert to label

- max_length:

  Max length allowed for a label to have (35)

## Value

A valid set of labels
