# Make profile labels

Make profile labels

## Usage

``` r
.profile_labels(upstream, downstream, mode)
```

## Arguments

- upstream:

  Basepairs upstream

- downstream:

  Basepairs downstream

- mode:

  Plot mode (stretch, start, end, center)

## Value

Array of strings
