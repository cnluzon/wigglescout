# Test if an array has repeated element

Test if an array has repeated element

## Usage

``` r
.repeated_elements(array)
```

## Arguments

- array:

  An array of char

## Value

TRUE if all elements in the array are unique
