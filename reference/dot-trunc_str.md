# Truncate a string to a max length

Truncate a string to a max length

## Usage

``` r
.trunc_str(s, max_length = 35)
```

## Arguments

- s:

  String to truncate

- max_length:

  Length to truncate s to (35)
