# Validate that group col exists in granges

Validate that group col exists in granges

## Usage

``` r
.validate_group_col(granges, group_col)
```

## Arguments

- granges:

  GRanges object to check

- group_col:

  Group column name. Usually, name.

## Value

Nothing (NULL) on success.
